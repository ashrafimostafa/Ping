rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open rules for a private two-person Ping app.
    // Random 6-char IDs are the only access control.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
